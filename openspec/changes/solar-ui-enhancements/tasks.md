## 1. Data Structure Update

- [x] 1.1 Update `planets` array in `script.js` to include `nameEn` and `infoEn` fields for all planets.

## 2. UI Enhancements (HTML/CSS)

- [x] 2.1 Add "Pause/Resume" button to `index.html`.
- [x] 2.2 Update `info-panel` in `index.html` to support dual-language display (Chinese and English labels).
- [x] 2.3 Add CSS styles for the pause button in `style.css`.
- [x] 2.4 Add `.paused` class style in `style.css` to set `animation-play-state: paused` for orbits.

## 3. Logic Implementation (JavaScript)

- [x] 3.1 Implement pause/resume toggle logic in `script.js`.
- [x] 3.2 Update `showPlanetInfo` function in `script.js` to populate both Chinese and English fields in the info panel.
- [x] 3.3 Ensure clicking planets still triggers the info panel and doesn't conflict with animation state.

## 4. Verification

- [x] 4.1 Verify the pause button correctly stops and resumes planetary movement.
- [x] 4.2 Verify the info panel displays correct bilingual information when clicking different planets.
